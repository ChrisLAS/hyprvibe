# Hyprland Configuration for Rvbee
# See https://wiki.hyprland.org/Configuring/Configuring-Hyprland/

# Source shared base configuration (use absolute path; ~ is not expanded by Hyprland)
source = /home/chrisf/.config/hypr/hyprland-base.conf

# Source host-specific monitor configuration
source = /home/chrisf/.config/hypr/hyprland-monitors-rvbee-120hz.conf

# Enforce 144Hz on login in case early monitor keyword is ignored
exec-once = sh -c "sleep 1 && hyprctl keyword monitor 'DP-1,2560x1080@144,0x0,1'"

# Rvbee-specific overrides and additions can go here
# For example:
# - Machine-specific keybindings
# - Hardware-specific settings
# - Laptop-specific optimizations 

# Rvbee-specific window rules can be added here if needed